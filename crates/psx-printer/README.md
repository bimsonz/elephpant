# psx-printer

AST → `.psx` pretty-printer. Walks a `psx-ast` module and produces canonically-formatted PHPScript source. Used by the LSP's document-formatting handler.

Part of the [PHPScript](https://github.com/bimsonz/elephpant) compiler workspace.
