# `hello` example

Exercises Phase 1–2 ground: type-annotated functions, optional parameters, `if`/`elseif`/`else`, `foreach`, array literals, hash-map literals (`["k" => "v"]`), nullable return types.

Run:

```bash
../../target/release/psx build main.psx
# writes dist/main.ts
```
