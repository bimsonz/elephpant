# `oop` example

Every Phase 3 OOP feature in one file: `interface` with constants, backed `enum`, `abstract class`, `readonly class`, `final class`, constructor property promotion, `extends`, `implements`, `parent::__construct`, `public static`, class constants, instance and static method calls.

Run:

```bash
../../target/release/psx build main.psx
```
