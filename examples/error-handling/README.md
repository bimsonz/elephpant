# `error-handling` example

Phase 5 control-flow primitives that aren't loops: `match` expressions (with comma-separated arms and `default`), ternary `?:`, the elvis `?:` short form, `throw`/`try`/`catch`/`finally`, and custom error classes.

Run:

```bash
../../target/release/psx build main.psx
```
