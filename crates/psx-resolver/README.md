# psx-resolver

PSR-4 namespace resolver for PHPScript (`.psx`). Reads `psx.json`; resolves `use Foo\Bar;` paths to relative ES module imports.

Part of the [PHPScript](https://github.com/bimsonz/elephpant) compiler workspace.
