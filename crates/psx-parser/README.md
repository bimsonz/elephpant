# psx-parser

Hand-rolled recursive-descent parser for PHPScript (`.psx`). Consumes [`psx-lexer`](https://crates.io/crates/psx-lexer) tokens; produces [`psx-ast`](https://crates.io/crates/psx-ast) nodes.

Part of the [PHPScript](https://github.com/bimsonz/elephpant) compiler workspace.
